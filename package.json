{
  "name": "meetingmind-root",
  "version": "1.0.0",
  "description": "Root package for running MeetingMind AI",
  "scripts": {
    "start": "concurrently \"npm run start:backend\" \"npm run start:frontend\"",
    "start:backend": "call backend\\.venv\\Scripts\\activate && uvicorn backend.main:app --reload",
    "start:frontend": "cd frontend && npm run dev",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && python -m venv .venv && call .venv\\Scripts\\activate && pip install -r requirements.txt"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
